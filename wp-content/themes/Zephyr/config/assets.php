<?php defined( 'ABSPATH' ) OR die( 'This script cannot be accessed directly.' );

unset( $config['font-awesome-duotone'] );
unset( $config['gravityforms'] );
unset( $config['bbpress'] );

return $config;
